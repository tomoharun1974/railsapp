<%= form_for(@book) do |f| %>
 <%= f.collection_select :publish,@books, :id, :publish %>
<% end %>
<%= form_for(@review) do |f| %>
 レビュー対象書籍：
 <%= f.grouped_collection_select :book_id, @authors, :books, :name, :id, :title %>
 <%= select_tag 'book', option_groups_from_collection_for_select(
  @authors, :books, :name, :id, :title, '2') %>
<% end %>

